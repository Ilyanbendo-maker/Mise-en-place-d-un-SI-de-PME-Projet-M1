# Configuration globale du control node Ansible (~/ansible/ansible.cfg)
[defaults]
inventory = inventory/hosts.yml
host_key_checking = False
remote_user = ansible
private_key_file = ~/.ssh/id_ed25519
vault_password_file = ~/.vault_pass
