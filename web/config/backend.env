# Variables d'environnement du backend Node.js (fichier .env, secrets epures)
DB_HOST=localhost
DB_USER=admin-cercueil
DB_PASSWORD=[REDACTED]
DB_NAME=cercueil_fun
PORT=3001
# Authentification de l'espace admin contre le RODC Active Directory, en LDAPS
LDAP_URL=ldaps://rodc01.cercueil.local:636
# Secret de signature des jetons de session (genere via crypto.randomBytes(32))
JWT_SECRET=[REDACTED]
